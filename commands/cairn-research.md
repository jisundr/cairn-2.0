---
description: Investigates a topic via parallel subagents into the active task folder.
argument-hint: <topic or question>
---

No topic given → ask for one.

Invoke `Skill(skill: "cairn:research")` with it. That skill owns topic resolution and the active-task-folder precondition — this command is only its entry point.
