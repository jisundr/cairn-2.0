---
name: readme
description: Structure for a README under docs/ — what it is, how to use it, how it's configured. Loaded by scribe when asked for a README.
---

# cairn:readme

Write under `docs/`, at a path matching what was asked — the project root is outside `scribe`'s scope, so this is a `docs/`-tree README, not the repo's own.

## What it is

One or two sentences: what this covers and who it's for.

## Usage

The steps or commands someone actually needs to use it.

## Configuration

Any options, flags, or files that change its behavior — omit if there are none.

Lead with what a reader needs in the next five minutes, not a history of decisions.
